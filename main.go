package main

import "fmt"

func main() {
	// Ler os arquivos da pasta migrations e listar os nomes
		// Verficar se todos os arquivos tem o pattern correto (V0_nome.sql)
		// Verificar se todos os SQLs estão corretos
		// Verificar se todos os arquivos estão na ordem correta e se não existe repetição
	
	// Executar as migrations
	// Salvar o resultado da execução das migrations
}